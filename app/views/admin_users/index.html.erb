<% provide(:title, "List of all users") %>
<h1>All users</h1>
<% if @users.any? %>
	<table class="table">
		<%= render partial: 'admin_users/user', collection: @users %>
	</table>
	<%= will_paginate @users %>
<% end %>