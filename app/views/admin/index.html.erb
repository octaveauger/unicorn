<% provide(title: 'Admin section') %>
<h1>Admin centre</h1>

<table class="table">
	<tr>
		<td>There are <%= Activity.count %> activities</td>
		<td><%= link_to "Manage activities", admin_activities_path, :class => 'btn btn-primary' %></td>
	</tr>
	<tr>
		<td>There are <%= User.count %> users</td>
		<td><%= link_to "Manage users", admin_users_path, :class => 'btn btn-primary' %></td>
	</tr>
</table>